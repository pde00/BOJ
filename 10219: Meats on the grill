#include <iostream>
using namespace std;

int t,h, w;
char ar[11][11];

int main() {
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	
	cin >> t;
	
	while (t--) {
		cin >> h >> w;
		for (int i = 0; i < h; i++) { for (int j = 0; j < w; j++) { cin >> ar[i][j]; } }
		for (int i = h - 1; i > -1; i--) { for (int j = 0; j < w; j++) { cout << ar[i][j]; } cout << '\n'; }
	}
}
