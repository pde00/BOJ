#include <iostream>
using namespace std;

int ar[5], sum;

int main() {
	for (int i = 0; i < 5; i++) {
		cin >> *(ar+i);
		if (ar[i] < 40) {
			ar[i] = 40;
		}
		sum += ar[i];
	}
	cout << sum / 5 << '\n';
}
